# findMypet 下一阶段任务计划：模板扩展与分步发布流程

## 目标

把当前“功能可用”的 H5 工具推进到“用户愿意填、愿意转发、能快速生成好海报”的阶段。

重点解决两个问题：
- 模板少：用户需要不同传播场景的海报模板，而不是只有一个默认样式。
- 表单压迫感强：现在字段一次性铺开太多，用户看到会觉得麻烦。

## 当前状态

- 已有匿名创建、分享页、管理页、图片上传、三套海报模板：`classic`、`alert`、`square`。
- 已支持手机局域网预览：`npm run dev:mobile`。
- 当前发布表单仍是单页大表单，字段密度高。

## 阶段 1：模板体系产品化

状态：complete

任务：
- 抽象模板元数据：`id`、名称、适用场景、画幅、推荐用途。
- 海报页模板选择从硬编码 Link 改成配置驱动。
- 增加模板预览说明，让用户知道每个模板适合什么场景。
- 保持当前 URL 兼容：`/poster/:id?template=classic|alert|square`。

验收：
- 新增模板只需要加组件和配置，不需要改多处页面逻辑。
- 模板选择区能清楚说明“朋友圈方图 / 强告示 / 通用打印”。
- `npm test`、`npm run lint`、`npm run build` 通过。

完成记录：
- 已新增 `posterTemplateOptions` 和 `getPosterTemplateHref`。
- 海报页模板选择已改为配置驱动。
- 模板选择卡片已展示画幅与适用场景。

## 阶段 2：再补 2 套高价值模板

状态：complete

建议模板：
- `minimal`：清爽社区通知模板，适合物业群、社区群，不那么压迫。
- `urgent`：极简大字模板，适合危急情况，如高速路、病危、需喂药。

边界：
- 不做复杂可视化模板编辑器。
- 不做拖拽排版。
- 不做用户自定义颜色字体。

验收：
- 模板总数达到 5 套。
- 每套模板都能下载 PNG、打印 PDF。
- 手机打开模板页不横向溢出。

完成记录：
- 新增 `minimal` 社区简洁模板，适合物业群、社区群和公告栏低压传播。
- 新增 `urgent` 危急大字模板，突出病危、需喂药、失明、车流危险等立即行动信息。
- 模板配置、URL 归一化和海报页渲染分支已支持 5 套模板。

## 阶段 3：分步发布流程

状态：complete

推荐步骤：
1. 快速必要信息：类型、宠物名/未知、宠物类型、丢失/拾获地点、联系方式。
2. 照片与特征：上传照片、毛色、体型、显著特征、AI 特征建议入口。
3. 时间与紧急程度：丢失/拾获时间、病危、需喂药、交通危险、老年/幼宠等。
4. 悬赏与补充：线索奖、找回奖、补充描述、防骗提示确认。
5. 预览生成：展示信息摘要、生成分享页和海报。

首版实现策略：
- 保留现有 API payload，不改数据库。
- 将 `NoticeForm` 拆成 step 组件，但最终提交仍复用原 `cleanPayload` 与 POST/PATCH。
- 第一步只校验最低必要字段，降低开始成本。
- 最后一步统一做完整校验。

验收：
- 用户首屏看到的字段不超过 6 个。
- 有进度提示，例如 “1/5 必填信息”。
- 可前进/后退，不丢已填内容。
- 创建成功后仍保存到 `/mine`。
- 编辑模式可复用分步流程，或先保持单页编辑。

完成记录：
- 创建模式已接入 5 步流程：必要信息、照片与描述、时间与紧急程度、悬赏与找回、预览生成。
- 第一步字段控制为 6 个：发布类型、宠物名称、宠物类型、详细地点、附近标志物、主联系方式。
- 编辑模式保持原单页表单，避免影响管理页修改路径。
- 第五步新增确认摘要，生成后仍复用原 POST、localStorage 和 `/mine` 记录逻辑。
- 移动端新增 sticky 操作栏，便于手机继续/返回。

## 阶段 4：分步流程体验优化

状态：complete

任务：
- 增加“跳过，稍后补充”策略。
- 图片上传在第二步出现，避免首屏压迫。
- 紧急标签用大按钮，不用普通字段。
- 悬赏输入改为元/金额显示，内部仍用分。
- 手机底部固定“下一步/生成”操作栏。

验收：
- 手机上单手能完成创建。
- 必填字段错误提示出现在当前步骤附近。
- 不需要滚动很长才能找到提交按钮。

完成记录：
- 照片、时间/紧急程度、悬赏与找回步骤已标记为可跳过，按钮文案显示“跳过，稍后补充”。
- 悬赏输入从“分”改为“元”，内部仍转换为最小货币单位保存。
- 风险标签按钮加大点击区域。
- 移动端已具备 sticky 上一步/下一步操作栏。

## 阶段 5：真实预览与回归

状态：complete

任务：
- 使用本地 dev:mobile 预览首页、创建页、管理页、三类海报。
- 创建一条测试寻宠和一条测试寻主。
- 验证生成链接、管理链接、模板切换、PNG 下载。
- 如条件允许，用手机浏览器实际走一遍。

验收：
- `npm test`、`npm run lint`、`npm run build` 通过。
- 首页、分享页、管理页、海报页 HTTP 200。
- 手机局域网地址可打开。

完成记录：
- `npm run dev:mobile` 已启动，局域网预览地址为 `http://192.168.10.33:3000`。
- 首页 `/`、我的启事 `/mine`、公开列表 `/api/notices` 返回 200。
- 使用本地测试启事 `vYPV3jRq` 验证分享页 `/notice/vYPV3jRq` 返回 200。
- 5 套海报入口均返回 200：classic、alert、square、minimal、urgent。
- 注意：本机 `127.0.0.1` 直连在一次内容 grep 时出现连接拒绝，改用 `localhost` 和 dev server 日志确认路由可达。

## 当前优先级

1. 阶段 5：完整预览回归。

## 暂不做

- 小程序。
- 站内私信。
- 完整账号体系。
- 模板拖拽编辑器。
- 复杂地图定位适配。
- 复杂 AI 自动生成全海报。
