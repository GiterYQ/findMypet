# findMypet 下一阶段发现记录

## 产品发现

- 当前工具的核心能力已经成型，但创建表单字段一次性暴露太多，会降低首屏转化。
- 用户真正想要的是“先生成一个能发出去的启事”，详细信息可以后补。
- 模板不是单纯美化，而是传播场景适配：
  - 强告示模板适合丢失急、需要震慑或快速传播。
  - 方图模板适合朋友圈/微信聊天转发。
  - 经典模板适合打印和普通分享。

## 技术发现

- 现有 `NoticeCreateInput` 已经足够支撑分步表单，不需要先改 schema。
- `NoticeForm` 当前承担过多职责：默认值、字段更新、图片上传、校验、提交、结果展示。
- 拆分步骤时，应该优先拆 UI 组件，不要动 service/API。
- 海报模板目前通过 URL 参数切换，已经适合继续扩展为配置驱动。

## 参考模板观察

- 用户提供的参考图核心不是精美，而是“极强视觉层级”：
  - 大红标题块。
  - 大字号关键信息。
  - 金额/处罚/行动指令强突出。
  - 图片只作为辅助，不是主体。
- 这类思路已经迁移成 `alert` 模板。

## 风险

- 如果继续堆字段，产品会变成“半个平台”，降低工具完成度。
- 如果模板配置继续散在页面里，后面加模板会越来越难维护。
- 如果分步表单每步都强校验，会重新制造阻力；应该区分“进入下一步校验”和“最终生成校验”。

## 建议

- 下一次开发先重构模板配置，再拆分表单。
- 分步表单首版只做创建流程；编辑流程可以先保持当前单页，避免一次性改太多。
- 每个阶段一个 commit，继续保持可回滚。
